{
    "name": "news-cat-meals",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "predeploy": "npm run build",
        "deploy": "gh-pages -d dist",
        "postbuild": "powershell Copy-Item dist/index.html dist/404.html"
    },
    "devDependencies": {
        "gh-pages": "^6.3.0",
        "vite": "^5.0.0"
    }
}
